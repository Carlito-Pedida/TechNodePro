<% if obj.errors.any? %>
<h3>The following errors prevented the <%= obj.class.name.downcase %> from being created!</h3>
<hr>
<ul>
    <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
    <% end %>
</ul>
</div>
<% end %>